import React from "react"

interface ReservationFormProps {
  seats: number
  seatsTotal: number
}

const ReservationForm = ({ seats, seatsTotal }: ReservationFormProps) => {
  return <div>ReservationForm</div>
}

export default ReservationForm
